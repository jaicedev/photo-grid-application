import { UPDATE_GRAYSCALE } from "../actions/types";

const initialState = {
  grayscale: 0,
};

export default function (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
