import { UPDATE_PHOTO_COUNT } from "../actions/types";

const initialState = {
  photos: 1,
};

export default function (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
